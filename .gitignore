# Local .terraform directories
# These directories are created by 'terraform init' and contain the providers and modules for the project.
# They can be large and should be fetched fresh on each system.
.terraform/
**/.terraform/*

# .tfstate files
# These files are used to store the state of your managed infrastructure.
# While you are using a remote S3 backend, it's a best practice to ignore local state files
# in case they are accidentally created.
*.tfstate
*.tfstate.*

# Crash log files
# These are created if Terraform crashes.
crash.log
crash.*.log

# Exclude all .tfvars files to prevent sensitive variables from being committed.
# If you need to version default values for variables, use a .tfvars.example file.
*.tfvars
*.tfvars.json

# Ignore override files as they are meant to be used locally and not shared.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
# Plan files can be large and may contain sensitive information.
*.tfplan

# The .terraform.lock.hcl file is generated by 'terraform init' and locks provider versions.
# It is recommended to commit this file to version control to ensure that all team members
# are using the same provider versions. Therefore, it is NOT included here.
# .terraform.lock.hcl

# OS-specific files
.DS_Store
Thumbs.db 